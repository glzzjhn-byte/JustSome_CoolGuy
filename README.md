# JustSome_CoolGuy

## Build

```
ninja
```

Dependencies are managed via `vcpkg.json` (manifest mode).

## Project Layout

- `src/` — source files
- `include/JustSome_CoolGuy/` — public headers
- `tests/` — test sources, wired into CMake via `enable_testing()`
